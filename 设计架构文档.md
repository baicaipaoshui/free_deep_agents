# IAV AI Safety Platform — FuSa 多智能体编排器架构设计方案

> 基于 DeepAgents 源码二次开发 · Skills/Hooks 参考 automotive-claude-code-agents  
> 技术栈：Python + FastAPI + DeepAgents/LangGraph + Dify(RAG) + Next.js  
> Version 4.0 | 2026.04 | CONFIDENTIAL

---

## 1. 项目背景与目标

### 1.1 项目背景

IAV AI Safety Platform 面向整车功能安全开发全生命周期，构建一组专用 AI 智能体。本系统基于 LangChain 的 [DeepAgents](https://github.com/langchain-ai/deepagents) 框架（`libs/deepagents/`）进行二次开发，通过三个 SubAgent（HARA、FMEA、FTA）自动化 ISO 26262 安全分析工作。

Skills 的内容和组织方式参考 [automotive-claude-code-agents](https://github.com/theja0473/automotive-claude-code-agents) 项目（75+ 领域技能类别、115 份标准参考文档），将其汽车安全领域知识体系适配到 DeepAgents 原生的 `SkillsMiddleware`（`deepagents/middleware/skills.py`）中。

### 1.2 设计目标

- **效率提升**：传统 2 小时人工分析 → 2 分钟 AI 辅助生成 + 人工审核
- **合规保障**：全链路审计（LangSmith）、追溯链、强制 Human-in-the-Loop 审批门
- **可扩展性**：新增分析类型 = 新 SubAgent 定义 + `SKILL.md` + Hook 函数
- **源码对齐**：所有扩展严格基于 DeepAgents 已有机制，不发明新抽象

---

## 2. DeepAgents 源码结构与我们的扩展点

### 2.1 DeepAgents 源码关键模块

基于你提供的 `libs/` 目录树，DeepAgents 的核心模块如下：

```
libs/deepagents/deepagents/
├── __init__.py              # 导出 create_deep_agent()
├── graph.py                 # LangGraph StateGraph 构建，Agent 主循环
├── _models.py               # 模型配置和初始化
├── base_prompt.md           # 内置系统提示词
├── middleware/
│   ├── __init__.py          # 导出所有 Middleware
│   ├── subagents.py         # SubAgentMiddleware — 子代理管理
│   ├── async_subagents.py   # AsyncSubAgentMiddleware — 异步子代理
│   ├── skills.py            # SkillsMiddleware — SKILL.md 加载
│   ├── filesystem.py        # FilesystemMiddleware — 文件读写工具
│   ├── memory.py            # MemoryMiddleware — AGENTS.md 记忆加载
│   ├── summarization.py     # SummarizationMiddleware — 上下文压缩
│   ├── patch_tool_calls.py  # PatchToolCallsMiddleware — 修复悬空调用
│   └── _utils.py            # Middleware 工具函数
└── backends/
    ├── protocol.py          # BackendProtocol — 统一存储接口
    ├── state.py             # StateBackend — 图状态内存储
    ├── store.py             # StoreBackend — LangGraph Store 持久化
    ├── composite.py         # CompositeBackend — 路径路由存储
    ├── filesystem.py        # FilesystemBackend — 本地文件系统
    └── ...
```

CLI 层的关键文件：

```
libs/cli/deepagents_cli/
├── hooks.py                 # Hooks 机制实现
├── skills/
│   ├── load.py              # Skills 加载逻辑
│   └── commands.py          # Skills 相关命令
├── built_in_skills/
│   ├── remember/SKILL.md    # 内置记忆技能
│   └── skill-creator/SKILL.md  # 技能创建器
└── subagents.py             # CLI 层子代理管理
```

### 2.2 我们的扩展策略

**原则：不 fork 修改 DeepAgents 核心代码，只通过其公开 API 扩展。**

| 扩展点 | DeepAgents 原生机制 | 我们的做法 |
|--------|---------------------|------------|
| 子代理 | `SubAgentMiddleware` + `CompiledSubAgent` | 定义 HARA/FMEA/FTA 三个 SubAgent |
| 领域知识 | `SkillsMiddleware` 加载 `SKILL.md` | 编写汽车安全领域的 `SKILL.md` 文件 |
| RAG 检索 | 自定义 Tool（`@tool` 装饰器） | 封装 Dify Knowledge API 为 Tool |
| 安全门 | `hooks.py` 机制 | 注册自定义 Hook 函数 |
| 文件操作 | `FilesystemMiddleware` + `BackendProtocol` | 使用 `CompositeBackend` 路由持久化 |
| 记忆 | `MemoryMiddleware` 加载 `AGENTS.md` | 编写项目级 `AGENTS.md` 定义记忆 |
| 审计追踪 | LangSmith（DeepAgents 原生集成） | 配置 LangSmith tracing |

---

## 3. 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 前端 | Next.js | SSR、流式渲染、React 生态 |
| 后端 API | FastAPI | 异步原生、与 DeepAgents 同 Python 生态 |
| Agent 框架 | DeepAgents 0.4.x | Middleware 架构、SubAgent、Skills、Hooks 原生支持 |
| 图引擎 | LangGraph | DeepAgents 底层依赖（`graph.py` 使用 `StateGraph`） |
| RAG | Dify | 可视化知识库管理、开箱即用文档解析 |
| 数据库 | PostgreSQL | 项目数据、审计日志 |
| 缓存 | Redis | 会话状态、LangGraph checkpoint |
| 对象存储 | MinIO | 文档附件、生成报告 |
| LLM | 可配置 | 通过 `_models.py` 的 `init_chat_model()` 统一切换 |
| 追踪 | LangSmith | DeepAgents 原生集成，Agent 全链路追踪 |

---

## 4. 系统架构详细设计

### 4.1 编排器初始化

基于 `create_deep_agent()` 组合 Middleware：

```python
# backend/app/agent/orchestrator.py
from deepagents import create_deep_agent
from deepagents.middleware import (
    SubAgentMiddleware,
    SkillsMiddleware,
    FilesystemMiddleware,
    MemoryMiddleware,
)
from deepagents.backends import CompositeBackend, StateBackend, StoreBackend
from app.agent.subagents import hara_subagent, fmea_subagent, fta_subagent

orchestrator = create_deep_agent(
    model="claude-sonnet-4-5-20250929",
    system_prompt="你是 IAV AI Safety Platform 的安全分析编排器。"
                  "根据用户请求，调用对应的安全分析子代理（HARA/FMEA/FTA）。",
    middleware=[
        SkillsMiddleware(skills_dir="skills/common/"),
        MemoryMiddleware(),  # 加载 AGENTS.md 项目记忆
        SubAgentMiddleware(
            subagents=[hara_subagent, fmea_subagent, fta_subagent],
        ),
        FilesystemMiddleware(
            backend=lambda rt: CompositeBackend(
                default=StateBackend(rt),
                routes={"/reports/": StoreBackend(rt)},  # 报告持久化
            ),
        ),
    ],
)
```

**说明**：
- `SkillsMiddleware` 自动扫描 `skills/common/` 目录下的 `SKILL.md`，将技能描述注入 system prompt
- `MemoryMiddleware` 从 `AGENTS.md` 加载项目级记忆（上下文背景、约束条件等）
- `SubAgentMiddleware` 注册三个子代理为 task tool，编排器通过工具调用分发任务
- `FilesystemMiddleware` 使用 `CompositeBackend`，`/reports/` 路径下的文件通过 `StoreBackend` 持久化跨线程

### 4.2 SubAgent 定义

每个 SubAgent 通过字典定义，与 DeepAgents 的 `SubAgent` spec 格式对齐：

```python
# backend/app/agent/subagents.py
from langchain_core.tools import tool
from deepagents.middleware import SkillsMiddleware

# === Dify RAG 工具（所有 SubAgent 共用） ===
@tool
def search_knowledge_base(query: str, collection: str = "iso26262") -> str:
    """从 Dify 知识库检索相关标准文档、历史案例和失效模式。
    collection 可选：iso26262_hara, iso26262_fmea, iso26262_fta, failure_modes, historical_cases
    """
    import httpx
    resp = httpx.post(
        f"{DIFY_API_URL}/v1/knowledge/retrieve",
        json={"query": query, "dataset_id": COLLECTION_MAP[collection]},
        headers={"Authorization": f"Bearer {DIFY_API_KEY}"},
    )
    results = resp.json().get("records", [])
    return "\n---\n".join([r["content"] for r in results[:5]])

# === ASIL 查表工具 ===
@tool
def asil_lookup(severity: int, exposure: int, controllability: int) -> str:
    """根据 S/E/C 值查 ISO 26262 ASIL 等级（QM/A/B/C/D）。"""
    table = { ... }  # ISO 26262 ASIL lookup table
    return table.get((severity, exposure, controllability), "QM")

# === SubAgent 定义 ===
hara_subagent = {
    "name": "hara_analyst",
    "description": (
        "执行 HARA（危害分析与风险评估）。包括：Item Definition 解析、"
        "失效模式识别、车辆级危害推导、S/E/C/O 估计、ASIL 定级、安全目标生成。"
        "严格按照 ISO 26262 Part 3 流程执行。"
    ),
    "system_prompt": open("skills/hara/SKILL.md").read(),  # CoT 模板作为 system prompt
    "tools": [search_knowledge_base, asil_lookup],
    "middleware": [
        SkillsMiddleware(skills_dir="skills/hara/"),
    ],
}

fmea_subagent = {
    "name": "fmea_analyst",
    "description": (
        "执行 FMEA（失效模式与影响分析）。包括：结构分析、失效模式挖掘、"
        "RPN 风险评估、纠正措施建议。支持 RAG 知识增强。"
    ),
    "system_prompt": open("skills/fmea/SKILL.md").read(),
    "tools": [search_knowledge_base],
    "middleware": [
        SkillsMiddleware(skills_dir="skills/fmea/"),
    ],
}

fta_subagent = {
    "name": "fta_analyst",
    "description": (
        "执行 FTA（故障树分析）。包括：顶事件定义、AND/OR 逻辑门递归分解、"
        "底事件映射（关联 FMEA 结果）、可选定量分析。"
    ),
    "system_prompt": open("skills/fta/SKILL.md").read(),
    "tools": [search_knowledge_base],
    "middleware": [
        SkillsMiddleware(skills_dir="skills/fta/"),
    ],
}
```

**关键设计点**：
- 每个 SubAgent 的 `system_prompt` 直接加载对应的 `SKILL.md`，其中包含完整的结构化 CoT 模板
- SubAgent 可以有自己的 `middleware`（比如各自的 `SkillsMiddleware` 加载领域专用技能）
- Dify RAG 检索封装为标准 `@tool`，SubAgent 在推理过程中按需调用
- SubAgent 执行完毕后返回精简结果给编排器，保持编排器上下文窗口干净

### 4.3 使用 CompiledSubAgent（高级场景）

当需要对 Pipeline 流程做精细控制（比如强制顺序执行、阶段间状态传递、验证门）时，可以将 SubAgent 包装为 LangGraph 子图：

```python
# backend/app/agent/graphs/hara_pipeline.py
from langgraph.graph import StateGraph, END
from deepagents.middleware.subagents import CompiledSubAgent
from typing import TypedDict, Annotated
import operator

class HARAState(TypedDict):
    messages: Annotated[list, operator.add]
    item_definition: str
    failure_modes: list[dict]
    hazards: list[dict]
    seco_ratings: list[dict]
    asil_results: list[dict]
    safety_goals: list[dict]
    current_stage: str
    awaiting_approval: bool

def create_hara_graph():
    workflow = StateGraph(HARAState)
    
    workflow.add_node("parse_item_definition", parse_item_definition)
    workflow.add_node("identify_failure_modes", identify_failure_modes)
    workflow.add_node("derive_hazards", derive_hazards)
    workflow.add_node("estimate_seco", estimate_seco)
    workflow.add_node("classify_asil", classify_asil)
    workflow.add_node("generate_safety_goals", generate_safety_goals)
    
    workflow.add_edge("parse_item_definition", "identify_failure_modes")
    workflow.add_edge("identify_failure_modes", "derive_hazards")
    workflow.add_edge("derive_hazards", "estimate_seco")
    # S/E/C/O 后需要人工审批，用条件边控制
    workflow.add_conditional_edges(
        "estimate_seco",
        lambda state: "wait" if state["awaiting_approval"] else "classify_asil",
    )
    workflow.add_edge("classify_asil", "generate_safety_goals")
    workflow.add_edge("generate_safety_goals", END)
    
    workflow.set_entry_point("parse_item_definition")
    return workflow.compile()

# 封装为 CompiledSubAgent
hara_compiled = CompiledSubAgent(
    name="hara_analyst",
    description="执行完整 HARA Pipeline（6 阶段 + 验证门）",
    runnable=create_hara_graph(),
)
```

---

## 5. Skills 设计（基于 `SkillsMiddleware`）

### 5.1 DeepAgents 原生 Skills 机制

从源码 `deepagents/middleware/skills.py` 和 CLI 的 `cli/deepagents_cli/skills/load.py` 可知，SkillsMiddleware 按以下规则工作：

1. 扫描指定目录下的 `SKILL.md` 文件
2. 解析 SKILL.md 的 metadata（名称、描述、触发条件）
3. 将技能描述注入 Agent 的 system prompt
4. Agent 在推理时根据上下文自动激活相关技能

### 5.2 我们的 Skills 目录

参考 automotive-claude-code-agents 的 75+ 技能类别，聚焦 FuSa 领域：

```
skills/
├── common/                          # 通用安全技能
│   ├── iso26262-overview/
│   │   └── SKILL.md                 # ISO 26262 全生命周期概览
│   ├── asil-decomposition/
│   │   └── SKILL.md                 # ASIL 分解规则和约束
│   └── safety-case/
│       └── SKILL.md                 # Safety Case 构建指南
│
├── hara/                            # HARA 专用技能
│   ├── SKILL.md                     # HARA 主技能（CoT 模板）
│   ├── item-definition/
│   │   └── SKILL.md                 # Item Definition 解析指南
│   ├── failure-modes/
│   │   └── SKILL.md                 # 失效模式关键词模板
│   ├── hazard-derivation/
│   │   └── SKILL.md                 # 危害推导规则
│   ├── seco-estimation/
│   │   └── SKILL.md                 # S/E/C/O 评估标准和场景库
│   └── safety-goals/
│       └── SKILL.md                 # 安全目标生成模板
│
├── fmea/                            # FMEA 专用技能
│   ├── SKILL.md                     # FMEA 主技能（CoT 模板）
│   ├── structure-analysis/
│   │   └── SKILL.md                 # 系统结构分解方法
│   ├── failure-mining/
│   │   └── SKILL.md                 # 失效模式挖掘规则
│   ├── rpn-calculation/
│   │   └── SKILL.md                 # RPN 计算和阈值规则
│   └── corrective-actions/
│       └── SKILL.md                 # 纠正措施推荐模板
│
└── fta/                             # FTA 专用技能
    ├── SKILL.md                     # FTA 主技能（CoT 模板）
    ├── top-event/
    │   └── SKILL.md                 # 顶事件定义规则
    ├── gate-decomposition/
    │   └── SKILL.md                 # AND/OR 门分解模板
    └── basic-event-mapping/
        └── SKILL.md                 # 底事件映射规则（含 FMEA 交叉引用）
```

### 5.3 SKILL.md 格式

遵循 DeepAgents 内置技能的格式（参考 `cli/deepagents_cli/built_in_skills/remember/SKILL.md`）：

```markdown
---
name: hara-failure-modes
description: "HARA 失效模式识别：基于功能描述和关键词模板系统性生成失效模式"
---

# HARA 失效模式识别技能

## 触发条件
当用户请求识别失效模式、分析功能故障、或执行 HARA 步骤 2 时激活。

## 方法论（结构化 CoT）

### 步骤 1：提取功能描述
从 Item Definition 中提取所有功能及其接口、工况。

### 步骤 2：应用关键词模板
对每个功能，系统性应用以下四个失效模式关键词：
- **Loss of function**（功能丧失）：该功能完全不可用
- **Degradation of function**（功能退化）：功能部分可用但性能下降
- **Unintended activation**（非预期激活）：功能在不应激活时激活
- **Incorrect output**（输出错误）：功能输出了错误的值或信号

### 步骤 3：安全相关性初判
对每个失效模式，标注是否与安全相关，给出理由。

### 步骤 4：输出格式
以结构化表格输出：
| 功能 | 失效模式 | 关键词类别 | 安全相关性 | 理由 |

## 参考标准
ISO 26262-3:2018, Clause 7
```

---

## 6. Hooks 设计（基于 `hooks.py`）

### 6.1 DeepAgents 原生 Hooks 机制

从源码 `cli/deepagents_cli/hooks.py` 可知，DeepAgents CLI 已有 hooks 注册和执行机制。我们在此基础上注册自定义 Hook 函数。

### 6.2 我们的 Hooks

Hook 分为两类：**自动校验**（无需人工介入）和**人工审批**（暂停等待确认）。

```python
# backend/app/hooks/safety_hooks.py

def asil_gate_hook(state: dict) -> dict:
    """HARA: S/E/C/O 估计完成后，强制人工确认 ASIL 定级。"""
    seco = state.get("seco_ratings", [])
    if seco:
        state["awaiting_approval"] = True
        state["approval_type"] = "asil_confirmation"
        state["approval_detail"] = {
            "message": "请确认以下 S/E/C/O 评估结果和 ASIL 定级",
            "data": seco,
        }
    return state

def rpn_gate_hook(state: dict) -> dict:
    """FMEA: RPN > 阈值时触发人工审核。"""
    RPN_THRESHOLD = 100
    high_risk = [fm for fm in state.get("failure_modes", [])
                 if fm.get("rpn", 0) > RPN_THRESHOLD]
    if high_risk:
        state["awaiting_approval"] = True
        state["approval_type"] = "rpn_review"
        state["approval_detail"] = {
            "message": f"{len(high_risk)} 个失效模式 RPN > {RPN_THRESHOLD}，需人工审核",
            "data": high_risk,
        }
    return state

def fta_logic_check_hook(state: dict) -> dict:
    """FTA: 自动校验逻辑门一致性（无需人工）。"""
    tree = state.get("fault_tree", {})
    errors = validate_gate_consistency(tree)
    if errors:
        state["validation_errors"] = errors
        state["needs_rework"] = True
    return state

def fta_xref_fmea_hook(state: dict) -> dict:
    """FTA: 检查底事件是否都能映射到 FMEA 失效模式。"""
    basic_events = state.get("basic_events", [])
    fmea_modes = state.get("fmea_failure_modes", [])
    unmapped = [e for e in basic_events if e["id"] not in
                {m["id"] for m in fmea_modes}]
    if unmapped:
        state["unmapped_events"] = unmapped
        state["warning"] = f"{len(unmapped)} 个底事件未映射到 FMEA"
    return state
```

---

## 7. Dify RAG 集成

### 7.1 集成方式

Dify RAG 不作为 Middleware，而是封装为标准 LangChain `@tool`。SubAgent 在推理过程中按需调用该 Tool 检索相关知识。这比 Middleware 注入更灵活——Agent 自主决定何时检索、检索哪个知识库。

### 7.2 知识库组织

| 知识库 (Dify Dataset) | 内容 | 关联 SubAgent |
|----------------------|------|---------------|
| `iso26262_hara` | ISO 26262 Part 3 + HARA 案例 | HARA |
| `iso26262_fmea` | ISO 26262 Part 5 FMEA 章节 + FMEA 模板 | FMEA |
| `iso26262_fta` | ISO 26262 Part 5 FTA 章节 + 方法论 | FTA |
| `failure_modes` | 按子系统分类的失效模式库（制动/转向/ADAS/动力总成） | 共用 |
| `historical_cases` | 已完成的安全分析案例 | 共用 |
| `company_standards` | IAV 内部检查表、开发指南 | 共用 |

---

## 8. 记忆架构（基于 `MemoryMiddleware` + Backends）

### 8.1 项目级记忆（AGENTS.md）

DeepAgents 的 `MemoryMiddleware`（`deepagents/middleware/memory.py`）从 `AGENTS.md` 文件加载记忆上下文。我们为每个项目创建独立的 `AGENTS.md`：

```markdown
# Project: BMS 电池管理系统功能安全开发

## 项目背景
- OEM: 某新能源汽车制造商
- 系统: ASIL-D 电池管理系统 (BMS)
- 范围: 过充/过放/过温保护功能的功能安全开发

## 约束条件
- 目标 ASIL: ASIL-D
- 适用标准: ISO 26262:2018 2nd Edition
- 已有输入: 系统架构图 V2.1、功能需求规格 V1.3

## 历史决策
- 2026-03-15: 确认 BMS 过温保护功能为安全相关功能
- 2026-03-20: HARA 初版完成，识别 12 个危害场景
```

### 8.2 文件持久化（CompositeBackend）

使用 DeepAgents 的 `CompositeBackend`（`deepagents/backends/composite.py`），将不同路径路由到不同存储后端：

- `/reports/` → `StoreBackend`（LangGraph Store，跨线程持久化）
- 默认 → `StateBackend`（图状态内临时存储）

---

## 9. 前端设计（Next.js）

### 9.1 核心页面

| 页面 | 功能 |
|------|------|
| `/projects` | 项目列表、创建新项目（生成 `AGENTS.md`） |
| `/analysis/:id` | 分析工作台：表格 + Agent 面板 + Pipeline 进度 |
| `/approval` | 审批队列：ASIL 确认、RPN 审核、安全目标签核 |
| `/knowledge` | 知识库管理：对接 Dify 界面 |

### 9.2 AI 交互模式

- **手动模式**：用户手动填写分析表格，AI 不介入
- **Tab 补全**：AI 生成建议以浅色展示，按 Tab 接受（参考 IAV 提案 Slide 15）
- **Agent 委托**：一键委托 SubAgent 执行完整 Pipeline，关键节点暂停等待审批
- **流式输出**：WebSocket 实时展示 CoT 推理步骤、Pipeline 阶段进度、工具调用过程

---

## 10. 项目目录结构

```
iav-fusa-platform/
├── frontend/                              # Next.js
│   ├── app/
│   │   ├── projects/
│   │   ├── analysis/[id]/
│   │   ├── approval/
│   │   └── knowledge/
│   ├── components/
│   │   ├── AgentPanel.tsx
│   │   ├── PipelineProgress.tsx
│   │   └── ApprovalDialog.tsx
│   └── package.json
│
├── backend/                               # FastAPI + DeepAgents
│   ├── app/
│   │   ├── main.py                        # FastAPI 入口
│   │   ├── api/                           # REST + WebSocket
│   │   │   ├── projects.py
│   │   │   ├── analysis.py
│   │   │   └── approval.py
│   │   ├── router/
│   │   │   └── intent.py                  # 两级意图路由
│   │   ├── agent/
│   │   │   ├── orchestrator.py            # create_deep_agent() 编排器
│   │   │   ├── subagents.py               # 三个 SubAgent 定义 + Tools
│   │   │   └── graphs/                    # CompiledSubAgent 用的 LangGraph 子图
│   │   │       ├── hara_pipeline.py
│   │   │       ├── fmea_pipeline.py
│   │   │       └── fta_pipeline.py
│   │   ├── hooks/
│   │   │   └── safety_hooks.py            # asil_gate, rpn_gate, logic_check 等
│   │   ├── models/                        # SQLAlchemy 数据模型
│   │   └── services/
│   │
│   ├── skills/                            # SKILL.md 文件（SkillsMiddleware 加载）
│   │   ├── common/
│   │   │   ├── iso26262-overview/SKILL.md
│   │   │   ├── asil-decomposition/SKILL.md
│   │   │   └── safety-case/SKILL.md
│   │   ├── hara/
│   │   │   ├── SKILL.md                   # HARA 主技能 + CoT 模板
│   │   │   ├── item-definition/SKILL.md
│   │   │   ├── failure-modes/SKILL.md
│   │   │   ├── hazard-derivation/SKILL.md
│   │   │   ├── seco-estimation/SKILL.md
│   │   │   └── safety-goals/SKILL.md
│   │   ├── fmea/
│   │   │   ├── SKILL.md
│   │   │   ├── structure-analysis/SKILL.md
│   │   │   ├── failure-mining/SKILL.md
│   │   │   ├── rpn-calculation/SKILL.md
│   │   │   └── corrective-actions/SKILL.md
│   │   └── fta/
│   │       ├── SKILL.md
│   │       ├── top-event/SKILL.md
│   │       ├── gate-decomposition/SKILL.md
│   │       └── basic-event-mapping/SKILL.md
│   │
│   ├── knowledge-base/                    # 导入 Dify 的原始文档
│   │   ├── iso26262/
│   │   ├── iso21448/
│   │   └── templates/
│   │
│   ├── pyproject.toml
│   └── tests/
│
├── docker-compose.yml                     # PostgreSQL + Redis + MinIO + Dify
└── README.md
```

---

## 11. 未来演进路线

| 触发条件 | 引入内容 | DeepAgents 扩展点 | 工作量 |
|----------|----------|-------------------|--------|
| 多子系统并行 FMEA | Fan-out 模式 | LangGraph `Send()` 在子图内 | 2-3 周 |
| ASIL 定级分歧仲裁 | Debate/Council | 新增仲裁 SubAgent | 3-4 周 |
| SubAgent > 10 | 三级意图路由 | 利用 SkillsMiddleware 技能描述自动路由 | 2 周 |
| 长耗时分析（跨系统 FMEA） | 异步 SubAgent | `AsyncSubAgentMiddleware` 原生支持 | 2-3 周 |
| 新增 DFA / FMEDA / SOTIF | 新 SubAgent | 新 SubAgent 定义 + SKILL.md | 2-3 周/个 |
| 自动生成 Safety Case | 跨 Agent 证据聚合 | 新编排工作流 | 4-6 周 |
| CLI 开发者工具 | 终端交互 | 复用 `libs/cli/` 做 FuSa CLI | 2 周 |

---

## 12. 关键设计决策

### 决策 1：不 fork DeepAgents，只通过公开 API 扩展

DeepAgents 的 Middleware 架构（SubAgent / Skills / Filesystem / Memory / Summarization）已经覆盖了我们的所有需求。RAG 通过 Tool 集成、安全门通过 hooks 注册、记忆通过 `AGENTS.md` 管理。不修改源码意味着可以无痛升级 DeepAgents 版本。

### 决策 2：RAG 用 Tool 而非 Middleware

V3 方案中写了 `DifyRAGMiddleware` 在 `before_model` 中注入检索结果，但这不符合 DeepAgents 的 Middleware 模式（它们通过扩展 state schema 和提供 tools 工作，不是 before/after 钩子）。正确做法是将 Dify 检索封装为 `@tool`，让 Agent 自主决定何时调用。

### 决策 3：Skills 用 SKILL.md 原生格式

V3 方案中写了 YAML 格式的 Skills，但 DeepAgents 的 `SkillsMiddleware` 原生识别的是 `SKILL.md` 格式（参考 `cli/deepagents_cli/built_in_skills/remember/SKILL.md`）。直接使用原生格式，不需要额外适配。

### 决策 4：CompiledSubAgent 作为 Pipeline 的实现方式

对于需要严格顺序控制的分析流程（HARA 6 阶段），使用 `CompiledSubAgent` 封装 LangGraph `StateGraph`。对于不需要严格流程控制的场景，直接使用字典定义的 SubAgent 即可。两种方式可以混用。

---

## 13. 总结

V4 方案严格对照 DeepAgents 源码结构，所有扩展都通过框架的公开 API 实现：

- **SubAgent** → `SubAgentMiddleware` + `CompiledSubAgent`（源码：`middleware/subagents.py`）
- **领域知识** → `SkillsMiddleware` + `SKILL.md`（源码：`middleware/skills.py`、`cli/skills/load.py`）
- **RAG** → `@tool` 封装 Dify API（不造新 Middleware）
- **安全门** → Hooks 机制（源码：`cli/hooks.py`）+ LangGraph 条件边
- **记忆** → `MemoryMiddleware` + `AGENTS.md`（源码：`middleware/memory.py`）
- **持久化** → `CompositeBackend` 路由存储（源码：`backends/composite.py`）
- **审计** → LangSmith 原生集成