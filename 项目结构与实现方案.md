# IAV AI Safety Platform — 项目结构与实现方案

> 基于 DeepAgents + LangGraph + Dify RAG + FastAPI + Next.js
> 实现 ISO 26262 功能安全分析（HARA / FMEA / FTA）的多智能体平台
> Version 1.0 | 2026.04

---

## 1. 仓库整体布局

```
free_deep_agents/
├── apps/
│   └── iav-fusa-platform/          # FuSa 多智能体应用
│       ├── backend/                # FastAPI + DeepAgents 后端
│       └── frontend/               # Next.js 前端
├── libs/
│   ├── deepagents/                 # DeepAgents SDK 源码（本地 editable 安装）
│   └── cli/                        # CLI 工具 + 内置 Skills 参考实现
├── AGENTS.md                       # Monorepo 级开发规范
├── README.md
└── 设计架构文档.md                  # V4 完整架构设计方案
```

### 设计原则

- **不 fork DeepAgents**：只通过公开 API（SubAgent / Skills / Hooks / Middleware）扩展
- **RAG 用 Tool**：Dify Knowledge API 封装为 `@tool`，Agent 按需调用，不注入 Middleware
- **SKILL.md 原生格式**：与 `SkillsMiddleware` 格式完全对齐
- **审计全链路**：LangSmith tracing 覆盖所有 Agent 调用

---

## 2. 后端目录结构

```
backend/
├── app/
│   ├── main.py                     # FastAPI 入口，路由注册，LangSmith 初始化
│   ├── config.py                   # 环境变量配置
│   ├── api/
│   │   ├── analysis.py             # 分析会话 REST + WebSocket 流
│   │   ├── approval.py             # 人工审批门管理
│   │   └── projects.py             # 项目 CRUD
│   ├── agent/
│   │   ├── orchestrator.py         # DeepAgents 编排器（单例）
│   │   ├── subagents.py            # 三个 SubAgent 定义 + 共享工具
│   │   ├── stream_bus.py           # 异步事件总线（Pipeline → WebSocket）
│   │   └── graphs/
│   │       ├── hara_pipeline.py    # HARA LangGraph 图（6 阶段）
│   │       ├── fmea_pipeline.py    # FMEA LangGraph 图（4 阶段）
│   │       └── fta_pipeline.py     # FTA LangGraph 图（4 阶段 + 校验循环）
│   ├── hooks/
│   │   └── safety_hooks.py         # 4 个安全门 Hook 函数
│   ├── models/
│   │   └── schemas.py              # Pydantic 数据模型（全部 API 契约）
│   └── router/
│       └── intent.py               # 两级意图路由（关键词分类）
├── skills/
│   ├── common/                     # 通用 FuSa 技能（编排器加载）
│   │   ├── SKILL.md
│   │   ├── iso26262-overview/SKILL.md
│   │   ├── asil-decomposition/SKILL.md
│   │   └── safety-case/SKILL.md
│   ├── hara/                       # HARA 专用技能（hara_analyst 加载）
│   │   ├── SKILL.md                # 主技能：6 阶段流程 + 3 个对话审批门
│   │   ├── item-definition/SKILL.md
│   │   ├── failure-modes/SKILL.md
│   │   ├── hazard-derivation/SKILL.md
│   │   ├── seco-estimation/SKILL.md
│   │   └── safety-goals/SKILL.md
│   ├── fmea/                       # FMEA 专用技能（fmea_analyst 加载）
│   │   ├── SKILL.md
│   │   ├── structure-analysis/SKILL.md
│   │   ├── failure-mining/SKILL.md
│   │   ├── rpn-calculation/SKILL.md
│   │   └── corrective-actions/SKILL.md
│   └── fta/                        # FTA 专用技能（fta_analyst 加载）
│       ├── SKILL.md
│       ├── top-event/SKILL.md
│       ├── gate-decomposition/SKILL.md
│       └── basic-event-mapping/SKILL.md
├── knowledge-base/                 # 导入 Dify 的原始文档（ISO 26262 / 模板）
├── projects/                       # JSON 文件持久化（项目数据 + AGENTS.md）
├── AGENTS.md                       # 项目级记忆文件（MemoryMiddleware 加载）
├── pyproject.toml
└── .env.example
```

---

## 3. 核心调用链

```
用户请求（HTTP POST / WebSocket）
  │
  ▼
app/api/analysis.py
  ├── route_request()          → 意图分类（hara / fmea / fta / full）
  ├── _run_analysis()          → 启动后台异步任务
  │     │
  │     ▼
  │   get_orchestrator()       → 单例 DeepAgents 编排器
  │     create_deep_agent(
  │       subagents = [hara_subagent, fmea_subagent, fta_subagent],
  │       skills    = [SKILLS_COMMON_DIR],   # 注入通用 FuSa 知识
  │       memory    = [AGENTS_MD_PATH],      # 注入项目背景记忆
  │     )
  │     │
  │     ▼
  │   orchestrator.ainvoke(messages)
  │     │
  │     ├── 调用 hara_analyst（SubAgent）
  │     │     system_prompt = skills/hara/SKILL.md
  │     │     tools = [search_knowledge_base, asil_lookup]
  │     │     skills = [SKILLS_HARA_DIR]
  │     │
  │     ├── 调用 fmea_analyst（SubAgent）
  │     │     tools = [search_knowledge_base]
  │     │
  │     └── 调用 fta_analyst（SubAgent）
  │           tools = [search_knowledge_base]
  │
  └── stream_bus → WebSocket 广播实时事件
        事件类型：progress / tool_start / tool_end /
                  approval_required / complete / error
```

---

## 4. 关键模块详解

### 4.1 编排器（orchestrator.py）

```python
create_deep_agent(
    model     = MODEL,                    # 默认 anthropic:claude-sonnet-4-6
    subagents = [hara, fmea, fta],        # 三个专业分析 SubAgent
    skills    = [SKILLS_COMMON_DIR],      # ISO 26262 通用知识
    memory    = [AGENTS_MD_PATH],         # 项目背景：系统名称、约束、历史决策
    backend   = LocalShellBackend(),
)
```

编排器职责：接收用户自然语言请求，识别分析类型，将任务分发给对应 SubAgent。

支持 MiniMax 等代理：若设置了 `OPENAI_API_BASE`，自动切换到 `ChatOpenAI` 实例。

### 4.2 SubAgent 定义（subagents.py）

三个 SubAgent 均为字典格式（DeepAgents 新 API），共享两个工具：

**共享工具：**

| 工具 | 说明 |
|------|------|
| `search_knowledge_base(query, collection)` | 调用 Dify Knowledge API，返回 top-5 文档段落。collection 可选：`iso26262_hara / iso26262_fmea / iso26262_fta / failure_modes / historical_cases / company_standards` |
| `asil_lookup(severity, exposure, controllability)` | 查内置 ASIL 表（ISO 26262-3 Table 4），输入 S1-S3 / E1-E4 / C1-C3，返回 QM/ASIL-A/B/C/D |

**SubAgent 对比：**

| SubAgent | tools | skills 目录 | system_prompt 来源 |
|----------|-------|------------|-------------------|
| `hara_analyst` | search_knowledge_base + asil_lookup | skills/hara/ | skills/hara/SKILL.md |
| `fmea_analyst` | search_knowledge_base | skills/fmea/ | skills/fmea/SKILL.md |
| `fta_analyst` | search_knowledge_base | skills/fta/ | skills/fta/SKILL.md |

### 4.3 SKILL.md 的作用

每个 SubAgent 的 `system_prompt` 直接加载对应目录的主 `SKILL.md`，子目录的 `SKILL.md` 由 `SkillsMiddleware` 渐进式注入（Agent 需要时读取）。

**SKILL.md 加载机制：**
```
SkillsMiddleware
  → 扫描 skills/hara/ 目录下所有 SKILL.md
  → 解析 YAML frontmatter（name / description）
  → 将技能列表注入 system prompt（渐进式披露）
  → Agent 需要详情时读取 SKILL.md 正文
```

**SKILL.md 格式规范（DeepAgents 原生格式）：**
```markdown
---
name: skill-name          # 必须与目录名一致，小写字母/数字/连字符
description: "一句话描述"
---

# 技能正文（Markdown）
```

### 4.4 LangGraph Pipeline（graphs/）

三个 Pipeline 均为 `LangGraph StateGraph`，目前以 `CompiledSubAgent` 形式定义，**但尚未接入 DeepAgents 主调用链**（当前 orchestrator 使用的是 dict 格式的 SubAgent，不是 CompiledSubAgent）。

**HARA Pipeline（6 阶段）：**

```
parse_item_definition
  → identify_failure_modes
    → derive_hazards
      → estimate_seco ──[awaiting_approval=True]──→ END（等待人工确认）
                       ──[approved]──────────────→ classify_asil
                                                    → generate_safety_goals → END
```

| 节点 | 职责 | 触发事件 |
|------|------|---------|
| parse_item_definition | 提取最后一条 HumanMessage 作为 Item Definition | progress |
| identify_failure_modes | 4 关键词模板（Loss/Degradation/Unintended/Incorrect）| progress |
| derive_hazards | 失效模式 × 工况 → 车辆级危险事件 | progress |
| estimate_seco | S/E/C/O 评估，**设 awaiting_approval=True** | progress + **approval_required** |
| classify_asil | 调用 asil_lookup 工具查表定级 | progress |
| generate_safety_goals | 生成 ISO 26262 安全目标（每个 ASIL≥A 危害对应一条）| progress |

**FMEA Pipeline（4 阶段）：**

```
analyze_structure
  → mine_failure_modes
    → calculate_rpn ──[rpn > RPN_THRESHOLD]──→ END（等待人工审核）
                     ──[all ok]──────────────→ recommend_actions → END
```

**FTA Pipeline（4 阶段 + 校验循环）：**

```
define_top_event
  → decompose_gates
    → map_basic_events
      → validate_logic ──[errors && iterations < 3]──→ decompose_gates（重试）
                        ──[ok || iterations >= 3]────→ END
```

### 4.5 安全门 Hooks（safety_hooks.py）

四个 Hook 函数作为 LangGraph 节点的后处理步骤：

| Hook | 触发条件 | 类型 | 效果 |
|------|---------|------|------|
| `asil_gate_hook` | `seco_ratings` 非空 | 人工门 | `awaiting_approval=True`，`approval_type="asil_confirmation"` |
| `rpn_gate_hook` | 存在 `rpn > RPN_THRESHOLD` 的失效模式 | 人工门 | `awaiting_approval=True`，`approval_type="rpn_review"` |
| `fta_logic_check_hook` | AND/OR 门子节点数 < 2 | 自动校验 | `validation_errors` 填充，`needs_rework=True` |
| `fta_xref_fmea_hook` | FTA 底事件无 FMEA 对应 | 信息告警 | `unmapped_events` 填充，生成 warning 消息 |

### 4.6 审批流程（approval.py）

```
Pipeline 触发 approval_required 事件
  → WebSocket 推送到前端
    → 前端弹出 ApprovalDialog
      → 工程师确认/驳回
        → POST /api/approval/{id}/approve 或 /reject
          → 会话状态更新
            → （approve）继续推进 Pipeline
            → （reject）标记 CANCELLED
```

### 4.7 意图路由（router/intent.py）

两级关键词匹配，无需 LLM 调用，纯规则：

```python
# 第一级：分析类型
route_request(text) → {"analysis_type": "hara"|"fmea"|"fta"|"full", "hara_stage": ...}

# 第二级（仅 HARA）：识别具体阶段
classify_hara_stage(text) → "item_definition"|"failure_modes"|...|"unknown"
```

### 4.8 事件总线（stream_bus.py）

Pipeline 内部事件 → WebSocket 客户端的桥接层，基于 `contextvars.ContextVar` 绑定当前会话：

```python
# Pipeline 节点内调用（无需传 session_id）
stream_bus.emit_current({"type": "progress", "stage": "estimate_seco", "message": "..."})

# 外层绑定（analysis.py 启动后台任务时）
stream_bus.bind(session_id)
```

---

## 5. 数据模型

### 核心 Schema 关系

```
ProjectCreate/Response
  └─ AnalysisRequest/Response
       ├─ HARAResult
       │    ├─ SECORating  (scenario, S, E, C, ASIL, rationale)
       │    └─ SafetyGoal  (id, description, ASIL, associated_hazard, safe_state, ftti)
       ├─ FMEAResult
       │    ├─ FailureModeEntry  (id, component, FM, S, O, D, RPN, current_controls)
       │    └─ CorrectiveAction  (failure_mode_id, action, revised_S/O/D/RPN)
       └─ FTAResult
            ├─ FaultTreeNode  (id, label, node_type, children, probability, fmea_ref)
            └─ CutSet         (id, basic_events, order, probability)
```

### FaultTreeNode node_type 枚举

| 值 | 含义 |
|----|------|
| `top_event` | 根节点（唯一） |
| `intermediate_event` | 中间事件（可继续分解）|
| `and_gate` | AND 门（所有子节点同时触发）|
| `or_gate` | OR 门（任一子节点触发）|
| `basic_event` | 叶节点（不可继续分解，映射到 FMEA FM ID）|

---

## 6. Skills 体系

### SKILL.md 清单

| 文件路径 | name | 职责摘要 |
|---------|------|---------|
| common/SKILL.md | fusa-common | ISO 26262 通用知识、ASIL 等级、核心原则 |
| common/iso26262-overview/ | iso26262-overview | 12 部分结构、V 模型开发流程 |
| common/asil-decomposition/ | asil-decomposition | ASIL-D→B+B 等分解方案，独立性要求 |
| common/safety-case/ | safety-case | GSN 论证结构，证据收集，审计追踪 |
| hara/SKILL.md | hara-main | **6 阶段 Pipeline 指引 + 3 个对话审批门** |
| hara/item-definition/ | hara-item-definition | Stage 1：提取系统边界和功能清单 |
| hara/failure-modes/ | hara-failure-modes | Stage 2：4 关键词模板识别失效模式 |
| hara/hazard-derivation/ | hara-hazard-derivation | Stage 3：失效模式 × 工况 → 危险事件 |
| hara/seco-estimation/ | hara-seco-estimation | Stage 4：S/E/C/O 评分标准和审批门触发 |
| hara/safety-goals/ | hara-safety-goals | Stage 6：SafetyGoal schema 字段和写作规范 |
| fmea/SKILL.md | fmea-main | 4 阶段 Pipeline + RPN 审批门 + CorrectiveAction schema |
| fmea/structure-analysis/ | fmea-structure-analysis | Stage 1：层次树 + 接口矩阵 |
| fmea/failure-mining/ | fmea-failure-mining | Stage 2：标准失效模式类别库 + 三层影响链 |
| fmea/rpn-calculation/ | fmea-rpn-calculation | Stage 3：S/O/D 评分标准 + RPN 阈值处理 |
| fmea/corrective-actions/ | fmea-corrective-actions | Stage 4：根因分析 + CorrectiveAction 生成 |
| fta/SKILL.md | fta-main | 4 阶段 Pipeline + 校验循环 + FaultTreeNode schema |
| fta/top-event/ | fta-top-event | Stage 1：从 HARA 安全目标映射顶事件 |
| fta/gate-decomposition/ | fta-gate-decomposition | Stage 2：AND/OR 门选择决策树 + 校验前置自查 |
| fta/basic-event-mapping/ | fta-basic-event-mapping | Stage 3：fmea_ref 填充 + unmapped_events 处理 |

### HARA 主 SKILL.md 的对话审批门

HARA SKILL.md 在三个关键节点设置了**对话式审批门**，要求 Agent 主动停下来等用户确认，而不是一口气跑完：

| 门序号 | 位置 | 确认内容 |
|-------|------|---------|
| 1/3 | Stage 1 完成后 | 系统边界和功能清单是否正确完整 |
| 2/3 | Stage 4 完成后 | S/E/C/O 评分是否符合工程判断（直接影响 ASIL 等级）|
| 3/3 | Stage 6 完成后 | 安全目标签核（后续 FMEA/FTA 的输入基础）|

Agent 收到「确认」后才继续下一阶段；收到修改意见则调整后重新输出审批门再次等待。

---

## 7. 环境配置

### 必填配置

```bash
# LLM
DEEPAGENTS_MODEL=anthropic:claude-sonnet-4-6
ANTHROPIC_API_KEY=sk-ant-...

# Dify RAG（每个知识库填入对应 Dataset ID）
DIFY_API_URL=http://localhost:5001
DIFY_API_KEY=app-...
DIFY_DATASET_HARA=
DIFY_DATASET_FMEA=
DIFY_DATASET_FTA=
DIFY_DATASET_FAILURE_MODES=
DIFY_DATASET_HISTORICAL=
DIFY_DATASET_COMPANY=
```

### 可选配置

```bash
# 切换到 MiniMax 等 OpenAI 兼容代理
OPENAI_API_BASE=https://api.minimax.chat/v1
OPENAI_API_KEY=...

# 基础设施
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fusa
REDIS_URL=redis://localhost:6379
MINIO_ENDPOINT=localhost:9000

# LangSmith 审计追踪
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=ls__...
LANGSMITH_PROJECT=iav-fusa-platform

# FMEA 高风险阈值
RPN_THRESHOLD=100
```

### 基础设施启动

```bash
docker-compose up -d postgres redis minio dify
cd backend && uv sync
uv run uvicorn app.main:app --reload --port 8000
```

---

## 8. 前端结构

```
frontend/
├── app/
│   ├── layout.tsx              # 全局布局
│   ├── page.tsx                # 首页（重定向到 /projects）
│   ├── projects/               # 项目列表页
│   ├── analysis/[id]/          # 分析工作台（WebSocket 实时流）
│   ├── approval/               # 人工审批队列
│   └── knowledge/              # 知识库管理（对接 Dify）
├── components/
│   ├── AgentPanel.tsx          # WebSocket 实时流输出面板
│   ├── PipelineProgress.tsx    # Pipeline 阶段可视化进度条
│   └── ApprovalDialog.tsx      # 人工审批对话框（approve/reject）
└── lib/
    └── api.ts                  # API 客户端（封装所有 HTTP 调用）
```

### 前端交互模式

| 模式 | 说明 |
|------|------|
| 手动填表 | 用户手动填写 HARA/FMEA/FTA 分析表格，AI 不介入 |
| Agent 委托 | 一键委托 SubAgent 执行完整 Pipeline，关键节点暂停等待审批 |
| 流式输出 | WebSocket 实时展示 CoT 推理步骤、Pipeline 阶段进度、工具调用 |
| 追问 | POST /{id}/reply 追加对话，支持用户在任意阶段补充信息或提出修改 |

---

## 9. DeepAgents SDK 核心机制

### create_deep_agent() Middleware 栈顺序

```
TodoListMiddleware
SkillsMiddleware          ← skills 目录下所有 SKILL.md
FilesystemMiddleware      ← 文件读写工具
SubAgentMiddleware        ← 注册三个 SubAgent 为 task 工具
SummarizationMiddleware   ← 上下文压缩
PatchToolCallsMiddleware  ← 修复悬空工具调用
MemoryMiddleware          ← 加载 AGENTS.md 项目记忆
HumanInTheLoopMiddleware  ← 仅当 interrupt_on 配置时启用
```

### SubAgent 两种格式

**格式 A：dict SubAgent（当前使用）**
```python
{
    "name": "hara_analyst",
    "description": "...",
    "system_prompt": "...",   # SKILL.md 内容
    "model": "...",
    "tools": [...],
    "skills": ["skills/hara/"],
}
```
框架自动调用 `create_agent()` 编译，注入完整 Middleware 栈。

**格式 B：CompiledSubAgent（当前未接入主链路）**
```python
{
    "name": "hara_pipeline",
    "description": "...",
    "runnable": create_hara_graph(),   # 已编译的 LangGraph 图
}
```
框架直接使用 runnable，不注入额外 Middleware。

> **注意**：`hara_pipeline.py` 中导出的 `hara_compiled` 目前是孤立的，orchestrator 使用的是 dict 格式的 `hara_subagent`。两者目前互不相连。

### CompiledSubAgent 接入限制

SubAgentMiddleware 在调用任何 subagent 时，**不传入 config（无 thread_id，无 checkpointer）**：

```python
result = subagent.invoke(subagent_state)   # ← 无 config
```

因此，即使 CompiledSubAgent 内部配置了 `interrupt_before`，LangGraph interrupt 机制也无法正常工作（interrupt 会抛出异常而非 pause/resume）。如需在 SubAgent 层实现 human-in-the-loop，有以下方案：

| 方案 | 说明 | 复杂度 |
|------|------|--------|
| SKILL.md 对话审批门（当前） | Agent 靠 prompt 约束主动停下问用户，软约束 | 低 |
| 绕过 orchestrator，analysis.py 直接调图 | `analysis.py` 直接 invoke hara_pipeline，自己管理 thread_id，配合 `interrupt_before` 实现硬中断 | 中 |
| 改造框架 `_build_task_tool()` | 给 subagent invoke 传入 config，捕获 GraphInterrupt 并冒泡 | 高（框架层改动）|

---

## 10. 当前状态与待完成工作

### 已完成

- [x] FastAPI 后端基础框架（main/config/api/models）
- [x] DeepAgents 编排器 + 三个 SubAgent 定义
- [x] 三个 LangGraph Pipeline（HARA 6 阶段 / FMEA 4 阶段 / FTA 4 阶段 + 校验循环）
- [x] 安全门 Hooks（asil_gate / rpn_gate / fta_logic_check / fta_xref_fmea）
- [x] WebSocket 实时事件流（stream_bus + _StreamCallback）
- [x] 审批工作流接口（approval.py）
- [x] 意图路由（intent.py）
- [x] 项目管理 CRUD（projects.py）
- [x] 全部 SKILL.md 文件（17 个，对齐 Pipeline 阶段和 Schema 字段）
- [x] HARA SKILL.md 三个对话审批门
- [x] Next.js 前端页面结构

### 待完成

- [ ] **LangGraph Pipeline 真正接入主链路**：当前 hara_pipeline.py 等为孤立的 `hara_compiled`，需决定是否替换 dict SubAgent
- [ ] **approval.py 的 resume 逻辑**：审批通过后如何真正驱动 Pipeline 继续（当前只更新状态，未接 LangGraph resume）
- [ ] **FMEA / FTA SKILL.md 审批门**：FMEA 的高 RPN 审批门、FTA 的底事件未映射警告，目前只在 Pipeline 代码层面，SKILL.md 中缺乏对应的对话指引
- [ ] **知识库内容**：knowledge-base/ 目录仅有 `.gitkeep`，Dify 知识库内容需填充
- [ ] **数据库层**：PostgreSQL 数据模型未实现（当前 projects.py 用 JSON 文件替代）
- [ ] **前端组件实现**：组件文件存在但需完整实现
- [ ] **多轮对话 history 持久化**：分析会话的对话历史目前存在内存，需持久化
